import { Request } from "express";
import { Repository } from "typeorm";
import { AuditLogService } from "../../audit-log/audit-log.service";
import { User } from "../../users/user.entity";
import { ProjectDetectionProfile } from "../project-detection-profile.entity";
import { ProjectsService } from "../projects.service";
import { RepositoryWorkspaceService } from "./repository-workspace.service";
import { StackDetectionService } from "./stack-detection.service";
export declare class DeploymentProfileService {
    private readonly profileRepository;
    private readonly projectsService;
    private readonly repositoryWorkspaceService;
    private readonly stackDetectionService;
    private readonly auditLogService;
    constructor(profileRepository: Repository<ProjectDetectionProfile>, projectsService: ProjectsService, repositoryWorkspaceService: RepositoryWorkspaceService, stackDetectionService: StackDetectionService, auditLogService: AuditLogService);
    runDetection(user: User, projectId: string, req?: Request): Promise<{
        id: string;
        projectId: string;
        repositoryUrl: string;
        repositoryFullName: string;
        targetBranch: string;
        commitSha: string;
        ecosystem: string;
        language: string;
        framework: string;
        frameworkVariant: string;
        packageManager: string;
        runtimeVersion: string;
        buildCommand: string;
        startCommand: string;
        expectedPort: number;
        healthCheckPath: string;
        requiresDatabase: boolean;
        databaseType: string;
        requiresPersistentStorage: boolean;
        staticOutput: boolean;
        dockerfileRequired: boolean;
        hasDockerfile: boolean;
        selectedTemplate: string;
        confidence: string;
        detectionStatus: string;
        warnings: string[];
        errors: string[];
        createdAt: Date;
        updatedAt: Date;
    }>;
    getProfile(user: User, projectId: string): Promise<{
        id: string;
        projectId: string;
        repositoryUrl: string;
        repositoryFullName: string;
        targetBranch: string;
        commitSha: string;
        ecosystem: string;
        language: string;
        framework: string;
        frameworkVariant: string;
        packageManager: string;
        runtimeVersion: string;
        buildCommand: string;
        startCommand: string;
        expectedPort: number;
        healthCheckPath: string;
        requiresDatabase: boolean;
        databaseType: string;
        requiresPersistentStorage: boolean;
        staticOutput: boolean;
        dockerfileRequired: boolean;
        hasDockerfile: boolean;
        selectedTemplate: string;
        confidence: string;
        detectionStatus: string;
        warnings: string[];
        errors: string[];
        createdAt: Date;
        updatedAt: Date;
    }>;
    private saveProfile;
    private saveFailedProfile;
    private auditMetadata;
    private toProfileResponse;
}

import { Queue } from "bullmq";
import { Request } from "express";
import { Repository } from "typeorm";
import { AuditLogService } from "../../audit-log/audit-log.service";
import { User } from "../../users/user.entity";
import { StartPipelineRunDto } from "../dto/start-pipeline-run.dto";
import { ProjectDetectionProfile } from "../project-detection-profile.entity";
import { ProjectPreflightReport } from "../project-preflight-report.entity";
import { PipelineRunStatus, ProjectPipelineRun } from "../project-pipeline-run.entity";
import { ProjectPipelineEvent } from "../project-pipeline-event.entity";
import { ProjectsService } from "../projects.service";
import { PipelineJobData } from "./pipeline.types";
export declare class PipelineService {
    private readonly runRepository;
    private readonly eventRepository;
    private readonly profileRepository;
    private readonly preflightRepository;
    private readonly pipelineQueue;
    private readonly projectsService;
    private readonly auditLogService;
    constructor(runRepository: Repository<ProjectPipelineRun>, eventRepository: Repository<ProjectPipelineEvent>, profileRepository: Repository<ProjectDetectionProfile>, preflightRepository: Repository<ProjectPreflightReport>, pipelineQueue: Queue<PipelineJobData>, projectsService: ProjectsService, auditLogService: AuditLogService);
    startRun(user: User, projectId: string, dto: StartPipelineRunDto, req?: Request): Promise<{
        id: string;
        projectId: string;
        triggeredByUserId: number;
        preflightReportId: string;
        detectionProfileId: string;
        repositoryUrl: string;
        repositoryFullName: string;
        targetBranch: string;
        commitSha: string;
        imageName: string;
        imageTag: string;
        shortCommitSha: string;
        ecrRepositoryName: string;
        ecrImageUri: string;
        githubWorkflowRunId: string;
        githubWorkflowStatus: string;
        status: PipelineRunStatus;
        currentStage: string;
        startedAt: Date;
        completedAt: Date;
        failedAt: Date;
        errorMessage: string;
        createdAt: Date;
        updatedAt: Date;
    }>;
    listRuns(user: User, projectId: string): Promise<{
        id: string;
        projectId: string;
        triggeredByUserId: number;
        preflightReportId: string;
        detectionProfileId: string;
        repositoryUrl: string;
        repositoryFullName: string;
        targetBranch: string;
        commitSha: string;
        imageName: string;
        imageTag: string;
        shortCommitSha: string;
        ecrRepositoryName: string;
        ecrImageUri: string;
        githubWorkflowRunId: string;
        githubWorkflowStatus: string;
        status: PipelineRunStatus;
        currentStage: string;
        startedAt: Date;
        completedAt: Date;
        failedAt: Date;
        errorMessage: string;
        createdAt: Date;
        updatedAt: Date;
    }[]>;
    getRun(user: User, projectId: string, runId: string): Promise<{
        id: string;
        projectId: string;
        triggeredByUserId: number;
        preflightReportId: string;
        detectionProfileId: string;
        repositoryUrl: string;
        repositoryFullName: string;
        targetBranch: string;
        commitSha: string;
        imageName: string;
        imageTag: string;
        shortCommitSha: string;
        ecrRepositoryName: string;
        ecrImageUri: string;
        githubWorkflowRunId: string;
        githubWorkflowStatus: string;
        status: PipelineRunStatus;
        currentStage: string;
        startedAt: Date;
        completedAt: Date;
        failedAt: Date;
        errorMessage: string;
        createdAt: Date;
        updatedAt: Date;
    }>;
    listEvents(user: User, projectId: string, runId: string): Promise<ProjectPipelineEvent[]>;
    private toRunResponse;
}

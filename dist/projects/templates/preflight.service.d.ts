import { Request } from "express";
import { Repository } from "typeorm";
import { AuditLogService } from "../../audit-log/audit-log.service";
import { User } from "../../users/user.entity";
import { ProjectDetectionProfile } from "../project-detection-profile.entity";
import { ProjectEnvironmentVariable } from "../project-environment-variable.entity";
import { ProjectPreflightReport } from "../project-preflight-report.entity";
import { ProjectsService } from "../projects.service";
import { DockerTemplateEngineService } from "./docker-template-engine.service";
import { DevOpsTemplateDefinition } from "./devops-templates";
import { TemplateRegistryService } from "./template-registry.service";
export declare class PreflightService {
    private readonly reportRepository;
    private readonly profileRepository;
    private readonly envVarRepository;
    private readonly projectsService;
    private readonly templateRegistryService;
    private readonly dockerTemplateEngineService;
    private readonly auditLogService;
    constructor(reportRepository: Repository<ProjectPreflightReport>, profileRepository: Repository<ProjectDetectionProfile>, envVarRepository: Repository<ProjectEnvironmentVariable>, projectsService: ProjectsService, templateRegistryService: TemplateRegistryService, dockerTemplateEngineService: DockerTemplateEngineService, auditLogService: AuditLogService);
    generateReport(user: User, projectId: string, req?: Request): Promise<{
        id: string;
        projectId: string;
        detectionProfileId: string;
        templateKey: string;
        templateDisplayName: string;
        ecosystem: string;
        framework: string;
        frameworkVariant: string;
        packageManager: string;
        runtimeVersion: string;
        expectedPort: number;
        buildCommand: string;
        startCommand: string;
        healthCheckPath: string;
        hasDockerfile: boolean;
        dockerfileRequired: boolean;
        generatedDockerfile: string;
        report: Record<string, unknown>;
        validationStatus: string;
        warnings: string[];
        errors: string[];
        createdAt: Date;
        updatedAt: Date;
    }>;
    getReport(user: User, projectId: string): Promise<{
        id: string;
        projectId: string;
        detectionProfileId: string;
        templateKey: string;
        templateDisplayName: string;
        ecosystem: string;
        framework: string;
        frameworkVariant: string;
        packageManager: string;
        runtimeVersion: string;
        expectedPort: number;
        buildCommand: string;
        startCommand: string;
        healthCheckPath: string;
        hasDockerfile: boolean;
        dockerfileRequired: boolean;
        generatedDockerfile: string;
        report: Record<string, unknown>;
        validationStatus: string;
        warnings: string[];
        errors: string[];
        createdAt: Date;
        updatedAt: Date;
    }>;
    listTemplates(): DevOpsTemplateDefinition[];
    private buildReport;
    private saveReport;
    private getProfile;
    private addCheck;
    private isSupportedFramework;
    private validationStatus;
    private hasDockerignore;
    private audit;
    private toReportResponse;
}
